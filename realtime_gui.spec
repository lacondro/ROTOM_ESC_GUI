# -*- mode: python ; coding: utf-8 -*-

added_files = [ ("./icon/cpu.ico", '.')]
a = Analysis(
    ['realtime_gui.py'],
    pathex=['/Users/minku/Projects/ROTOM_CONTROL'],
    binaries=[],
    datas=added_files,
    hiddenimports=[],
    hookspath=[],
    hooksconfig={},
    runtime_hooks=[],
    excludes=[],
    noarchive=False,
    optimize=0,
)
pyz = PYZ(a.pure)

exe = EXE(
    pyz,
    a.scripts,
    a.binaries,
    a.datas,
    [],
    name='ROTOM CONTROL',
    debug=False,
    bootloader_ignore_signals=False,
    strip=False,
    upx=True,
    upx_exclude=[],
    runtime_tmpdir=None,
    console=False,
    disable_windowed_traceback=False,
    argv_emulation=False,
    target_arch=None,
    codesign_identity=None,
    entitlements_file=None,
)
app = BUNDLE(
    exe,
    name='ROTOM CONTROL.app',
    icon='./icon/cpu.ico',
    bundle_identifier=None,
)
